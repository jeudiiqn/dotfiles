Folders where to put these files

.asoundrc        > $HOME/
10-udisks2.rules > /etc/polkit-1/rules.d/
alacritty.toml   > $HOME/.config/alacritty/
asound.conf      > /etc/
xmonad.hs        > $HOME/.config/xmonad/
